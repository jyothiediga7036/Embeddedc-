#include<cstdint>
#include"stm32f407.h"
class friend_functon
{
	static unit32_t count;
	void gipo_ini()
	{

		RCC->AHB1ENR|=(1<<0);
		RCC->AHB1ENR|=(1<<3);

		for(uint32_t i=12;i<=15;i++)
		{
			GPIOD->MODER&=~(3<<(i*2));
			GPIOD->MODER|=(2<<(i*2));
		}
		GPIOA->MODER&=~(3<<0);
	}
public:
	friend_function()
	{
		count=0;
	}
	void status()
	{

	}
};
int main()
{

}
